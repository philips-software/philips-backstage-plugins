import { searchConfluenceFrontendPlugin } from './plugin';

describe('search-confluence-frontend', () => {
  it('should export plugin', () => {
    expect(searchConfluenceFrontendPlugin).toBeDefined();
  });
});
